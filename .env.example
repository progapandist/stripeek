# Fallback if you'd rather not use macOS Keychain.
# Copy to .env (gitignored) and source it: `set -a && source .env && set +a`
STRIPE_API_KEY=sk_test_replace_me

# Override where the test client points (default: http://localhost:4111).
# STRIPEEK_BASE=http://localhost:4111
